version: 0.2

phases: 
    pre_build:
        commands: 
            - echo "we are in the pre build phase"
            - npm install
    build:
        commands:
            - echo "we are in the build block"
            - npm run build

    artifacts:
      files:
        - "/**/*"
      discard-paths: no
      base-directory: build